I"(function() {
  $(function() {
    $(window).unload(function() {
      $.ajax({
        url: window.location.href + 'grounds/' + $('.ground').attr('id') + '/leave',
        type: 'get',
        success: function() {
          console.log('great');
        }
      });
    });
    $('#enter-ground-form').submit(function(e) {
      e.preventDefault();
      window.location.href = window.location.href + "grounds/" + ($('#enter-ground-form #name').val());
    });
    return $('.copy-ground-name').click(function() {
      var $temp;
      $temp = $('<input>');
      $('body').append($temp);
      $temp.val($('#ground-name').text()).select();
      document.execCommand('copy');
      $temp.remove();
      return $(this).html('Copied');
    });
  });

}).call(this);
:ET